"""Stage 2: turn `CostRow`s into public `Product`s (margin, offers, cleanup).

See `PLAN.md` (Fase 1) in `renovarte-parent` for the modules this package
will hold: `pricing.py`, `categories.py`, `images.py`, `offers.py`,
`build_catalog.py` (ported from `scripts/lib/*.ts` and `scripts/transform.ts`).
"""
