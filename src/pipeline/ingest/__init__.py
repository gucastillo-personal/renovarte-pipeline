"""Stage 1: download raw, untransformed data from a source (API/CSV/PDF).

See `PLAN.md` (Fase 1) in `renovarte-parent` for the modules this package
will hold: `serlaca_download.py` (ported from `scripts/ingest.ts`), etc.
"""
