"""Source adapters, each producing `CostRow`s from a raw input.

See `PLAN.md` (Fases 1-2) in `renovarte-parent` for the modules this package
will hold: `serlaca_api.py`, `csv_source.py` (ported from `scripts/lib/sources/*.ts`),
and `pdf_laca.py` (new — spec 0008, LACA price PDF via `pdfplumber`).
"""
